query Posts($limit: Int, $skip: Int, $category: String) {
  postCollection(
    limit: $limit
    skip: $skip
    where: { category: { slug: $category } }
  ) {
    total
    items {
      ...Post
    }
  }
}
